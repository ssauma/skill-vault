interface:
  display_name: "Skill Vault"
  short_description: "Diagnose and vault approved Codex skills"
  default_prompt: "Use $skill-vault to run a read-only context diagnosis. Do not change any skill without my explicit approval."
policy:
  allow_implicit_invocation: false
